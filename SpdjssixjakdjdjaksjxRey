--// Fish World Script Hub - RAYFIELD STABLE VERSION
--// Made by Rey

-- Safety clear
pcall(function()
    if game:GetService("CoreGui"):FindFirstChild("Rayfield") then
        game:GetService("CoreGui"):FindFirstChild("Rayfield"):Destroy()
    end
end)

-- Wait game loaded
repeat task.wait() until game:IsLoaded()

-- Load Rayfield
local Rayfield = loadstring(game:HttpGet("https://raw.githubusercontent.com/shlexware/Rayfield/main/source"))()

-- CONFIG
local KEY = "ReyScriptHubFishWorldSoOp"
local DISCORD = "https://discord.gg/G4n8yzBx2J"

local verified = false
local speed1, speed2 = 1, 1
local logs = {}

-- Helper
local function clean(str)
    return tostring(str or ""):gsub("%s+", "")
end

local function log(txt)
    table.insert(logs, os.date("[%H:%M:%S] ") .. txt)
    if #logs > 15 then table.remove(logs, 1) end
end

-- UI
local Window = Rayfield:CreateWindow({
    Name = "🎣 Fish World Hub",
    LoadingTitle = "Fish World",
    LoadingSubtitle = "by Rey",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "FishWorld",
        FileName = "Config"
    },
    Discord = {
        Enabled = true,
        Invite = "G4n8yzBx2J",
        RememberJoins = true
    },
    KeySystem = false
})

-- Tabs
local TabKey = Window:CreateTab("🔑 Key", 4483362458)
local TabMain = Window:CreateTab("⚡ Main", 4483362458)
local TabSettings = Window:CreateTab("⚙ Settings", 4483362458)
local TabConsole = Window:CreateTab("📜 Console", 4483362458)
local TabInfo = Window:CreateTab("ℹ Info", 4483362458)

-- KEY SYSTEM
local savedKey = ""

TabKey:CreateInput({
    Name = "Enter Key",
    PlaceholderText = "Paste your key here",
    RemoveTextAfterFocusLost = false,
    Callback = function(v)
        savedKey = clean(v)
    end
})

TabKey:CreateButton({
    Name = "Verify Key",
    Callback = function()
        if clean(savedKey) == clean(KEY) then
            verified = true
            Rayfield:Notify({
                Title = "Success",
                Content = "Key Verified!",
                Duration = 3
            })
            log("Key verified")
        else
            Rayfield:Notify({
                Title = "Error",
                Content = "Wrong key!",
                Duration = 3
            })
        end
    end
})

-- AUTO LOGIN
task.delay(1, function()
    if clean(savedKey) == clean(KEY) then
        verified = true
        Rayfield:Notify({
            Title = "Auto Login",
            Content = "Key loaded successfully",
            Duration = 3
        })
    end
end)

-- FUNCTIONS
local function Script1()
    task.wait(speed1)
    game:GetService("ReplicatedStorage").communication["9362e25a8d224fd987d0d3f50d8249bc"]:FireServer({
        Vector3.new(-269.9, 15.8, -262.7),
        Vector3.new(-0.43, 0, -0.9),
        1.95,
        false
    })
    log("Script 1 executed")
end

local function Script2()
    task.wait(speed2)
    game:GetService("ReplicatedStorage").communication["fc57ae91538d430fa1ff9a33be3bc18c"]:FireServer({
        true,
        Vector3.new(-274.2, 3.4, -274.8)
    })
    log("Script 2 executed")
end

-- MAIN
TabMain:CreateButton({
    Name = "▶ Execute All",
    Callback = function()
        if verified then
            task.spawn(function()
                Script1()
                Script2()
            end)
        end
    end
})

TabMain:CreateButton({
    Name = "⚡ Script 1",
    Callback = function()
        if verified then task.spawn(Script1) end
    end
})

TabMain:CreateButton({
    Name = "⚡ Script 2",
    Callback = function()
        if verified then task.spawn(Script2) end
    end
})

-- SETTINGS
TabSettings:CreateSlider({
    Name = "Delay Script 1",
    Range = {0, 10},
    Increment = 1,
    CurrentValue = 1,
    Callback = function(v) speed1 = v end
})

TabSettings:CreateSlider({
    Name = "Delay Script 2",
    Range = {0, 10},
    Increment = 1,
    CurrentValue = 1,
    Callback = function(v) speed2 = v end
})

-- CONSOLE
local console = TabConsole:CreateParagraph({ Title = "Logs", Content = "Waiting..." })

task.spawn(function()
    while true do
        console:Set("Content", table.concat(logs, "\n"))
        task.wait(0.3)
    end
end)

-- INFO
TabInfo:CreateParagraph({ Title = "Developer", Content = "Rey" })
TabInfo:CreateParagraph({ Title = "Discord", Content = DISCORD })

Rayfield:Notify({
    Title = "Loaded",
    Content = "Fish World Script Ready!",
    Duration = 4
})

print("✅ Fish World Script Loaded (Rayfield)")
